# План исправлений по ревью `a8c43ed..a55452b`

Источник находок: `REVIEW-2026-09-09.md` (нумерация `01-1`, `03-2`, `M3-1` и т.д.).
Статусы: ⬜ не начато · 🔄 в работе · ✅ сделано и проверено · ⏸ отложено с обоснованием.

Правила исполнения: минимальные дифы, каждое поведенческое изменение закрыто тестом,
никаких git-мутаций (коммит — решение владельца репозитория), правки параллельной сессии не откатываются.

## Волна 0 — до любой демонстрации и деплоя

| # | Задача | Находки | Статус |
|---|---|---|---|
| W0-1 | Захват плана `pending→claimed` до `runPlan`, исполнение и финал на `WithoutCancel`, тест на конкурентный approve | 02-1, 02-10 | ✅ |
| W0-2 | `list_trash`/`plan_empty_trash`: confine + ACL ≥viewer, cap после фильтрации | 02-2 | ✅ |
| W0-3 | `view_image`: `DecodeConfig` и предел пикселей до `Decode` | 02-3 | ✅ |
| W0-4 | `zipInto` пропускает служебные каталоги на любом уровне; `activity.project` — белый список полей без токена | 03-1, 03-2 | ✅ |
| W0-5 | `newfolder`/`rename`: проверка занятости цели → 409; тест «байты цели не тронуты» | 05-2 | ✅ |
| W0-6 | MySQL: индекс inline в 00039, настоящий FK в 00036, диалектно-нейтральные `Evict`/`Grant` | 01-1, 01-2/04-2, 04-3 | ✅ |
| W0-7 | Approve/Allow: флаг «в полёте» до await + обработка 409/404; сервер: `expect` до `send` | 07-1, 02-4 | ✅ |
| W0-8 | Глобальный error-sink + явная обработка в 12 «тихих» местах; TagsModal не сохраняет при упавшем `listTags` | 06-3, 06-4, 06-5, M2-6, M2-7 | ✅ |
| W0-9 | Демо-стенд без dev-хуков; правда в комментарии `vite.config.ts`; prod-сборка без `VITE_FILEX_API` падает; стенд собирает `packages/core` | M3-1, M3-13, M2-1, 08-1 | ✅ |

## Волна 1 — до того, как назвать это «готово»

| # | Задача | Находки | Статус |
|---|---|---|---|
| W1-1 | Контракт клиент↔сервер: scope поиска, `current` у версий, `opened_at` в Recent, `type`/`ttl_days` в корзине, `shared_by`/`shared_at`, 409→`DUPLICATE_NAME` только там, где он есть, `max` из ответа | 05-1, 05-3…05-6, 05-9, 05-10, 07-12 | ✅ |
| W1-2 | `EmptySelf` по страницам до конца, `guardedNode` для trashed по `StorageKey`, дедуп basename в zip | 03-3, 03-4, 03-5 | ✅ |
| W1-3 | Тесты «со стороны чужого»: zip, `PurgeSelf`, `Activity.List`, инструменты и планы от viewer; `SeedViewer` в testutil | 02-6, 03-6 | ✅ |
| W1-4 | Гонки: seq в `openPath`, токен в `openSession`/`send`, `abort()` в `removeSession`, `findLast` для карточек | 06-1, 07-2, 07-3, 07-7, 07-9 | ✅ |
| W1-5 | Стрим: SSE keepalive, watchdog на время approval, 429 → «занято», `ProviderError` вместо пустого успеха, `DeadlineExceeded` ≠ `Canceled` | 02-5, 07-4, 07-5, 01-3, 01-4 | ✅ |
| W1-6 | sqlite: `backend_mtime` в UTC на записи + тест с фиксированной зоной | 04-1 | ✅ |
| W1-7 | CI: job `app` (type-check, lint, vitest); e2e-профиль против живого filex; fixture demo-assets или fail-fast; PG-матрица store-тестов | 08-2, 08-3, 08-4, 04-4 | ✅ |
| W1-8 | Одна константа служебных имён и одна функция видимости для проекций, zip, ассистента, `ChildCounts`; гейт thumb-очереди | 04-10, M3-6, 03-10 | ✅ |
| W1-9 | Docs: 6 видов планов в CONFIGURATION, актуальный ASSISTANT, `purged:0` в BACKEND.md, `### Security` в CHANGELOG, ⚠ в BACKEND-GAP, e2e README | 01-5/08-5, 08-6, 08-7, 02-10, 01-6, M3-7, 08-8 | ✅ |
| W1-10 | Непроведённый UI: пять настроек, 9 ключей `assistant.tools.*`, экран `forbidden` в админке, `UNAUTHORIZED_EVENT` | 06-2, 07-6, 07-8, 05-11 | ✅ (с оговоркой N-14) |
| W1-11 | Загрузки: пул транспортов, один `refresh` на батч, фильтр директорий из OS-drop, ошибки `start()` | M3-2, 06-9 | ✅ |

## Волна 2 — гигиена

| # | Задача | Находки | Статус |
|---|---|---|---|
| W2-1 | Assistant backend hygiene: транзакции, каскады, cap до resolve, `MaxBytesReader`, redirect-guard, `quotaWords` | 01-7…01-12, 02-7…02-11, 04-5, 04-6, M3-3 | ✅ |
| W2-2 | Уборка: tmp-файл теста, логотипы, осиротевшие комментарии, catch-all маршрут, tiebreak в `ListTrashed`, ESCAPE в LIKE, e-mail как имя | 05-17, 08-13, 06-12, 03-11, 03-13, 03-12 | ⬜ |
| W2-3 | UI-полировка: `defaultPrevented` в TopBar, active «My files», drop на уровне окна, откат звезды, DestinationModal, гейт copy/cut/paste, DetailsPanel watchers | 06-6…06-11, 06-15, M2-8 | ✅ (06-16 и M2-9 отложены: файлы были заняты) |
| W2-4 | Data-layer: `awaitOp` с сигналом и ретраем, `uploadSession` только на 404/410, инвалидация `listStorages`, эвикция потомков, `RepositoryError` | 05-7, 05-8, 05-13, 05-14, M3-8 | ✅ |
| W2-5 | e2e: счётчики из `tree.json`, `page.clock`, одна версия Playwright, политика бейзлайнов, sourcemap из `dist/` | 08-9…08-12, 08-15, M2-5 | ✅ |

## Решения, принятые при исполнении

- **MySQL чиним, а не объявляем неподдерживаемым** (W0-6). Исправление совместимо с любым продуктовым ответом на вопрос «поддерживаем ли MySQL», а объявление — нет. Вопрос владельцу остаётся открытым.
- **Демо-стенд переведён с `vite dev` на `vite preview` собранного бандла** (W0-9). Это разом закрывает весь класс «dev-код на стенде», а не четыре названных хука, и не трогает e2e-сьют, которому dev-режим нужен. Цена: на стенде нет hot reload — для разработки использовать `pnpm --filter ./app dev` на хосте. Если стенд нужен именно как среда разработки, решение стоит пересмотреть.
- **Встраивание `app/` в Go-бинарь отложено** в волну 1+; в волне 0 только убирается ложь в комментарии и опасность dev-хуков на стенде.

**Волна 2 закрыта.** Итоговая проверка: backend — build и vet чисто, 67 пакетов с тестами зелёные, отдельные прогоны под `TZ=Asia/Istanbul` и `TZ=America/Los_Angeles` зелёные, `gofmt` по изменённым файлам чист; app — type-check и lint чисто, 50 файлов / 436 тестов, зелено в двух прогонах подряд, локали 581 = 581 = 581; web — type-check чист; e2e — проверка типов чиста, mock-профиль перечисляет 111 тестов, live-профиль против настоящего бэкенда проходит 9 из 9; файлы CI разбираются как корректный YAML.

**Волна 1 закрыта полностью.** Проверка после неё: backend — build и vet чисто, 67 пакетов с тестами зелёные, отдельный прогон под `TZ=Asia/Istanbul` зелёный; frontend — type-check и lint чисто, 38 файлов / 371 тест, локали 558 = 558 = 558; `e2e` — проверка типов чиста, mock-профиль перечисляет 111 тестов, live-профиль против настоящего бэкенда проходит.

**Волна 0 закрыта полностью.** Независимая проверка после неё: backend — build и vet чисто, 66 пакетов с тестами зелёные, `gofmt` по изменённым файлам чист; frontend — type-check и lint чисто, 38 файлов / 348 тестов (было 33/323), ключи локалей 557 = 557 = 557.

## Новое, найденное при исправлении

| # | Что | Откуда | Куда | Статус |
|---|---|---|---|---|
| N-1 | `vfMove` не проверяет занятость цели — та же тихая перезапись, что была в `vfRename` | агент W0-5 | волна 1 | ⬜ |
| N-2 | После фикса W0-5 переименование, меняющее только регистр (`a.txt`→`A.txt`), на регистронезависимых бэкендах (SMB, WebDAV) отвечает 409 | агент W0-5 | волна 1, решить продуктово | ⬜ |
| N-3 | Тексты 429 «ход уже идёт» и «лимит исчерпан» неразличимы без машиночитаемого кода | агент W1-4 | сервер ✅ (поле `code`, значения `busy` и `rate_limited`); клиенту развести тексты в трёх локалях — батч C | 🔄 |
| N-4 | После клиентской половины контракта (батч B) закрыть три ⚠ строки в `BACKEND-GAP.md`: Tags-scope, `shared_by`, тип записи корзины | агент W1-9 | батч B, финальный проход | ⬜ |
| N-6 | **Скачивание файла не работало против реального бэкенда**: клеился второй `?` в строку запроса | live-сьют агента W1-7 | ссылка стала обязанностью слоя данных | ✅ |
| N-9 | Пер-элементный пропуск недоступного члена в архиве недостижим через гранты: права монотонны по префиксу, поэтому при пройденной проверке корня все потомки тоже доступны. Живым остаётся только фильтр служебных путей. Если продукт хочет «скачать диск и получить только свою ветку» — это отдельное поведенческое изменение | агент W1-3 | решить продуктово | ⬜ |
| N-7 | `FloatingMenu` закрывается на любое событие прокрутки, включая горизонтальную прокрутку обёртки таблицы: при ширине окна меньше ~1500 px меню строки закрывается само через ~14 мс после открытия | агент W1-7 | волна 2 | ⬜ |
| N-8 | `release.yml` не проверяет `app` — сознательно: приложение не попадает ни в один релизный артефакт, джоба там ничего бы не гейтила | агент W1-7 | пересмотреть после встраивания в Go | ⏸ |
| N-10 | Остатки после смены семантики версий и контракта: снять `test.fail()` со сценария скачивания в live-сьюте; `e2e/tests/app/files-history.spec.ts` пиннит старую семантику («Current», две кнопки восстановления) и станет красным; `assistantStore` всё ещё держит собственную константу лимита бесед; `DeleteModal` говорит «в течение 30 дней» без опоры на серверный срок | агент W1-1 | батч C | ⬜ |
| N-11 | ✅ Те же текстовые сравнения дат в sqlite оставались в подсчётах за период, выборке просроченной корзины, подсчёте отказов реплик, диапазоне аудита и отметке staged-загрузок | агент W1-6 | закрыто финальным агентом | ✅ |
| N-12 | ✅ Служебный каталог миниатюр прятался ещё в ~10 местах сравнением по подстроке (протоколы S3/WebDAV/SFTP/NFS/FTP, кросс-хранилищные операции, просмотр ссылок, кеш архивов и `aiOps.List`); из-за этого файл `my.thumbsup.png` невидим ассистенту | агент W1-8 | закрыто финальным агентом | ✅ |
| N-13 | В админке `web` два тестовых файла падают по причинам окружения (нет каталога `site/`, нет `localStorage` в этом Node) — предшествует нашей работе, ни один файл `web/` не менялся | проверка после волны 1 | не чинить | ⏸ |
| N-14 | **Моя ошибка в задании — исправлена.** Агенту W1-10 было сказано «подключить ИЛИ убрать» пять неиспользуемых настроек, и он убрал четыре: папку загрузки по умолчанию, автооткрытие превью, поведение при конфликте имён и переключатель ассистента. Все четыре явно заданы в `DESIGN-SPEC.md` (секция «Storage & uploads» под Preferences и вкладка «AI assistant»), а соответствие спецификации — критерий приёмки проекта. Все четыре возвращены и подключены по-настоящему: папка по умолчанию отвечает там, где нет контекста загрузки (с проверкой на протухший адрес), автооткрытие превью срабатывает после одиночной загрузки, поведение при конфликте имён реализовано на клиенте в четырёх вариантах (сервер умеет только перезапись), переключатель ассистента закрывает панель | проверка после батча C | ✅ |
| N-15 | **Тринадцать из пятнадцати визуальных эталонов расходятся** из-за коммитов параллельной сессии `3b59d53` и `4b20f22` (новая графика в `Thumbnail.vue`, правки `TopBar.vue` и `DetailsPanel.vue`). Эталоны надо переснять тому, кто владеет этим изменением дизайна | прогон агента W2-5 | решение владельца | ⬜ |
| N-16 | В локальном каталоге демо-ассетов нет `illustration.webp`, который есть в закоммиченном `tree.json` — из-за этого один тест сетки видит 5 картинок вместо 6. Расхождение окружения, а не кода | прогон агента W2-5 | синхронизировать ассеты | ⬜ |
| N-17 | Хак поиска браузера в кеше (`app/scripts/shot.mjs`) стал мёртвым кодом после унификации версий Playwright — удалить вместе с ненужными импортами | агент W2-5 | волна 2, остаток | ⬜ |
| N-18 | ✅ Семья дефектов с форматом дат шире, чем чинили: любое переданное время, сравниваемое с колонкой серверного времени, включая фильтр по дате удаления в корзине. Окно шириной в сутки маскирует ошибку, окно «за сегодня» дало бы неверный ответ | агент W2-1 | закрыто | ✅ |
| N-19 | ✅ Чтение комментариев к узлу в обоих драйверах по-прежнему подставляет адрес электронной почты вместо пустого отображаемого имени — тот же класс утечки, что уже починили в трёх местах | агент W2-1 | закрыто | ✅ |
| N-20 | Сервер при загрузке умеет только перезапись со снятием версии; варианты «пропустить», «сохранить оба» и «спросить» реализованы на клиенте до отправки байт | агент W1-11 | к сведению, серверная поддержка — отдельная задача | ⏸ |
| N-21 | Вторая половина M3-8 не закрыта осознанно: стор ассистента читает класс ошибки транспорта, потому что ему нужны статус и код из тела. Честное решение — добавить в контракт репозитория сентинелы отказа ассистента и транслировать ошибку внутри двух методов; это изменение публичного контракта, а не мелкая правка | агент хвостов | бэклог | ⏸ |
| N-22 | Подпись «домашний диск» в боковой панели и в модалке выбора назначения всё ещё завязана на константу `main`, поэтому на демо-данных диск не получает подпись. Косметика, подсветку не задевает | агент хвостов | бэклог | ⬜ |
| N-23 | `06-16` (модалка доступа перечитывает весь листинг) и `M2-9` (наблюдатели панели деталей без обработки ошибок) отложены: файлы были заняты другим агентом в момент работы | батч C | бэклог | ⬜ |
| N-5 | Комментарии в коде расходятся с данными: `i18n.spec.ts` про отсутствие переключателя языка, `files-grid.spec.ts:3` про «8 файлов», `DEMO-ASSETS.md` и `playwright.app.config.ts` про «142 узла» (реально 9 файлов в корне, 143 файла, 151 запись) | агент W1-9 | волна 2 | ⬜ |

Миграция `00036` добавлена внутри ревьюируемого диапазона и ни в один релиз не входит — правка на месте корректна, доп. миграция не нужна (проверено `git tag --contains`).

## Регрессия, найденная и закрытая при исправлении

| # | Что | Статус |
|---|---|---|
| R-1 | Унификация служебных путей (W1-8) добавила маркер зашифрованной папки в общий список пропускаемых имён, и кросс-хранилищный перенос перестал его копировать. По документации `versioning/guard.go` потеря маркера делает зашифрованную папку нечитаемой навсегда — то есть копия на другом хранилище содержала бы шифротекст, который нечем открыть. Прежний список пропускал только корзину и миниатюры. Исправлено: маркер — единственное служебное имя, которое обязано ехать вместе с папкой. Регрессионный тест проверен на падение без исправления | ✅ |

## Открытые вопросы владельцу (из §9 отчёта)

Ответы могут изменить объём W0-6, W1-7 и W1-2; до ответов работаем по решениям выше.
