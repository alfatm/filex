# --- Build outputs ---
/bin/
/goreleaser-dist/
/backend/dist/
/backend/embed/web/
/backend/embed/admin/
/backend/embed/app/
/packages/*/dist/
/web/dist/
/web/dev-dist/
/desktop/dist/
/desktop/app/
/desktop/release/
# The bundled sync engine, produced at package time by scripts/fetch-cli.mjs.
# ⚠ ~150 MB, and on Linux it has no extension — the `*.exe` rule below does not
# catch it, so a plain `git add` would push the blob into history.
/desktop/build/bin/
/demo/dist/
/docs-site/.vitepress/dist/
/docs-site/.vitepress/cache/
/docs-site-dist.zip

# --- Go ---
*.exe
*.dll
*.so
*.dylib
*.test
*.out
coverage.txt
coverage.html
go.work
go.work.sum

# --- Node ---
node_modules/
.pnpm-store/
.turbo/
.cache/
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/
.yarn/

# --- IDE ---
.vscode/*
!.vscode/settings.json.example
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~
.claude

# --- Env ---
.env
.env.local
.env.*.local
.env.production
config.local.yaml

# --- Local dev DB / data ---
*.sqlite
*.sqlite3
*.sqlite-journal
*.db
*.db-shm
*.db-wal
/data/
/storage-data/
/backend/.dev-data/

# --- OS ---
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
# Windows reserved name — bash on Windows occasionally creates a file
# called "NUL" when scripts redirect to /dev/null via mistyped paths;
# git-on-Windows refuses to delete it cleanly so we just ignore it.
NUL

# --- Cypress artefacts ---
/web/cypress/videos/
/web/cypress/screenshots/
/web/cypress/downloads/

# --- Misc ---
*.pid
*.seed
*.tar.gz
*.tgz
.pnp.*
.rid

# Locally built server binaries. ⚠ These are ~160 MB each and `go build` in
# backend/ drops one here by default — a plain `git add` then fails the push on
# GitHub's blob limit, after the commit is already made. Cheaper to ignore them
# than to rewrite history again.
/backend/filex
/backend/filex.exe
/backend/filex-test
/backend/filex-test.exe

# Screenshots taken while checking the UI. They belong in the project's filex
# folder (see CLAUDE.md), never in the repo.
/shots/
/desktop-*.png

# E2E scratch: a local storage whose root fell back to ./fileman (lesson: a
# seeded storage takes config.path, not root — a wrong key silently lands every
# storage in the server working directory).
/fileman/
